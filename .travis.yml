sudo: required
dist: xenial
language: python
python:
  - "3.6"

before_install:
  - export PYTHONPATH="${TRAVIS_BUILD_DIR}"

install:
  - pip install -r requirements.txt

script:
  - do_stuff_1.sh &
  - do_stuff_2.sh &
  - wait $(jobs -p)
  - python do_stuff.py

after_success:
  - echo "All good!"
